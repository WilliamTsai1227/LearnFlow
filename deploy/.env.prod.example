# PostgreSQL
POSTGRES_USER=learnflow
POSTGRES_PASSWORD=改成強密碼
POSTGRES_DB=learnflow
POSTGRES_PORT=5432

# Docker Hub（必填）
DOCKERHUB_USER=你的dockerhub帳號
IMAGE_TAG=latest

# Backend pool
DB_POOL_MIN_SIZE=1
DB_POOL_MAX_SIZE=5

# Nginx 對外 port
NGINX_PORT=80
